import XCTest
@testable import CollectionViewExtension

final class CollectionViewExtensionTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //XCTAssertEqual(CollectionViewExtension().text, "Hello, World!")
    }
}
